{application, gibreel, [
	{description, "Gibreel Distributed Cache System"},
	{vsn, "0.5.0"},
	{modules, [
		gibreel_app, 
		gibreel_sup,
		gibreel,
		g_cache_sup,
		g_cache
	]},
	{registered, [
		gibreel,
		g_cache_sup
	]},
	{applications, [
		kernel, 
		stdlib, 
		columbo
	]},
	{mod, {gibreel_app, []}},
	{env, []}
]}.
