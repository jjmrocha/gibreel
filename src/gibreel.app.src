{application, gibreel, [
	{description, "Gibreel Distributed Cache System"},
	{vsn, "0.8.0"},
	{modules, [
	]},
	{registered, [
		gibreel,
		g_cache_sup
	]},
	{applications, [
		kernel, 
		stdlib, 
		columbo,
		cclock,
		async,
		event_broker
	]},
	{mod, {gibreel_app, []}},
	{env, []}
]}.
